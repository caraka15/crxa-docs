# Crxanode Docs

## Brand assets helper

Resize favicon / touch icons and refresh the manifest via:

```
pip install Pillow
python scripts/generate_brand_assets.py --manifest
```

Use `--icon filename=size` to add extra sizes (for example `--icon favicon.ico=64`).
